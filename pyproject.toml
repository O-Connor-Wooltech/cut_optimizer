[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cut-optimizer"
version = "0.1.2"
description = "1D cut list optimizer (mm) with kerf, CSV/XLSX import, PySide6 UI"
requires-python = ">=3.10"
